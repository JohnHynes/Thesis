#ifndef GPU_RAY_TRACING_TYPES_HPP_
#define GPU_RAY_TRACING_TYPES_HPP_

#include <glm/fwd.hpp>

using num = float;
using vec3 = glm::f32vec3;
using color = vec3;
using point3 = vec3;

#define CONST(x) static_cast<num> (x)

#endif