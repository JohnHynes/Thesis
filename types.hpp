#pragma once

#include <glm/fwd.hpp>
#include <glm/vec3.hpp>

using precision = double;
using vec3 = glm::f64vec3;
using color =  vec3;
using point3 = vec3;

#define CONST(x) static_cast<precision>(x)
